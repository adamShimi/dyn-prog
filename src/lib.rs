pub fn find_optimal(_prob : MDP) -> Policy {
  unimplemented!("Still everything to do!");
}

pub struct MDP;
pub struct Policy;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
